name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
      - run: uv python install 3.12
      - run: uv sync --dev
      - run: uv run ruff check rc_unit_economics/ tests/
      - run: uv run ruff format --check rc_unit_economics/ tests/
      - run: uv run python -m pytest tests/ -v
